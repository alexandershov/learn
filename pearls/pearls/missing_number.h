#ifndef PEARLS_MISSING_NUMBER_H
#define PEARLS_MISSING_NUMBER_H

#include <vector>

namespace ch2 {
//    finds a missing number in numbers (range is 1, max(numbers))
    int get_missing_number(std::vector<int> &numbers);
}

#endif //PEARLS_MISSING_NUMBER_H
